{
  "name": "drax",
  "version": "0.0.1",
  "description": "Drax the Deployer wants to savagely dispatch your static web content.",
  "scripts": {
    "debug": "nodemon --debug --cwd ./.dist --watch server --watch config server/index.js",
    "debug-brk": "nodemon --debug-brk --cwd ./.dist --watch server --watch config server/index.js",
    "production-dist": "gulp clean && gulp fresh-dist --type production",
    "start": "nodemon --cwd ./.dist --watch server --watch config server/index.js",
    "test": "gulp test",
    "watch": "gulp dev"
  },
  "author": "Andy Wilson",
  "repository": {
    "type": "git",
    "url": "https://github.com/wilsaj/drax"
  },
  "bugs": {
    "url": "https://github.com/wilsaj/drax/issues"
  },
  "homepage": "https://github.com/wilsaj/drax/issues",
  "license": "BSD",
  "devDependencies": {
    "assert": "^1.1.1",
    "async": "^0.9.0",
    "gulp": "^3.8.7",
    "gulp-mocha": "^0.5.2",
    "gulp-rimraf": "^0.1.0",
    "gulp-ruby-sass": "^0.7.1",
    "gulp-useref": "^0.6.0",
    "nodemon": "^1.2.1",
    "run-sequence": "^0.3.6",
    "should": "^4.0.4",
    "supertest": "^0.13.0"
  },
  "dependencies": {
    "bluebird": "^2.3.0",
    "body-parser": "^1.6.5",
    "consolidate": "^0.10.0",
    "express": "^4.8.3",
    "nconf": "^0.6.9",
    "swig": "^1.4.2",
    "underscore.string": "^2.3.3"
  }
}

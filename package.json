{
  "name": "drax",
  "version": "0.0.1",
  "description": "Drax the Deployer wants to savagely dispatch your static web content.",
  "scripts": {
    "dist": "gulp",
    "prod-dist": "gulp clean && gulp dist --type production",
    "debug": "nodemon --debug-brk --cwd ./.dist --watch server --watch config server/index.js",
    "start": "nodemon --cwd ./.dist --watch server --watch config server/index.js",
    "watch": "gulp dev"
  },
  "author": "Andy Wilson",
  "repository": {
    "type": "git",
    "url": "https://github.com/wilsaj/drax"
  },
  "bugs": {
    "url": "https://github.com/wilsaj/drax/issues"
  },
  "homepage": "https://github.com/wilsaj/drax/issues",
  "license": "BSD",
  "devDependencies": {
    "gulp": "^3.8.7",
    "gulp-rimraf": "^0.1.0",
    "gulp-ruby-sass": "^0.7.1",
    "nodemon": "^1.2.1"
  },
  "dependencies": {
    "express": "^4.8.3",
    "nconf": "^0.6.9"
  }
}